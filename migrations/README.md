# Database migrations

PostgreSQL migrations begin in Phase 2. The eventual baseline will preserve Dashboard revisions, Widget instances/projections, raw snapshots, Provider-native models, sync jobs, and settings as separate concepts.

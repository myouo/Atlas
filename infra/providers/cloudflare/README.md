# Optional Cloudflare deployment adapter

Cloudflare is an optional infrastructure provider, not a business dependency. Future templates in this directory may contain logical binding names and empty placeholders only; real account, zone, bucket, project, route, and domain values must remain in deployment secrets.
